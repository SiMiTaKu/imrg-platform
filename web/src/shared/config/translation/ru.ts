import { ALL_PAGES } from './allPages'

/**
 * ロシア語に訳し終えたページのパス。
 *
 * @remarks
 * 言語ごとにファイルを分けているのは、言語の作業を並行して進めても同じ行を取り合わないようにするため
 */
export const RU_TRANSLATED_PATHS: readonly string[] = ALL_PAGES
