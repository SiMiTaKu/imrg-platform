import { ALL_PAGES } from './allPages'

/**
 * スペイン語に訳し終えたページのパス（言語の接頭辞なし、末尾スラッシュ付き）。
 *
 * @remarks
 * すべてのページを訳し終えたので `ALL_PAGES` をそのまま使う
 */
export const ES_TRANSLATED_PATHS: readonly string[] = ALL_PAGES
