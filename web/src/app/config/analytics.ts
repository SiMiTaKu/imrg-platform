/**
 * アクセス解析を読み込むホスト名。**ここに挙げたホストだけが数えられる。**
 *
 * @remarks
 * ビーコンのトークン（PUBLIC_CF_BEACON_TOKEN）は本番だけのものではなく、
 * stg も Amplify の PR プレビューも同じものを積んでいる。そのため、
 * 読み込むホストを絞らないと、自分たちのアクセスが本番の数字に混ざる。
 *
 * 2026-09-16〜26 の実測では、Cloudflare Web Analytics に届いた 1,580 表示のうち
 * **18% が開発環境から**だった（stg・develop・main・pr-265・pr-276 のプレビュー）。
 *
 * 除外したいホストを並べる形（ブラックリスト）にはしないこと。
 * **PR プレビューは `pr-<番号>.<ID>.amplifyapp.com` でホスト名が毎回変わるため、
 * 数え上げられない。** 本番だけを通す形にしてある。
 *
 * 手元の開発サーバー（localhost）も、これで自然に外れる。
 */
export const ANALYTICS_HOSTS = ['imrg.work']
